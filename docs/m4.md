# M4 — Hero, Navigation, and Footer

## Goal

Build the top and bottom of the page: sticky navigation, hero section with headline and CTA, and footer with legal links and social channels. The page frame that wraps all other sections.

## PR Scope

- `index.html` — populated nav, hero, and footer sections
- `css/nav.css` — navigation styles (transparent → solid on scroll)
- `css/hero.css` — hero layout, headline, CTA
- `css/footer.css` — footer layout and links
- `js/nav.js` — scroll-based nav background transition

## Content Source

- Hero headline and subheading: from `content/hero.json` (M2)
- Footer contact channels: from `content/contact.json` (M2)
- Legal links: placeholder hrefs pointing to `#` until M11

## Tasks

- [ ] Build sticky nav with logo placeholder and anchor links
- [ ] Add language toggle to nav: flag buttons (🇪🇸 🇬🇧) with `ES` active by default — visual only, no JS wiring yet
- [ ] Add scroll-based nav background transition (transparent on hero, solid below)
- [ ] Build hero section: headline, subheading, CTA button, image placeholder (organic mask)
- [ ] Add botanical decorative accent to hero (CSS `::before` or inline SVG)
- [ ] Add scroll indicator (downward arrow)
- [ ] Build footer: brand name, tagline, social icons, privacy/cookie link placeholders
- [ ] Mobile hamburger menu (overlay, full-screen)
- [ ] CTA links to `#contacto` anchor
- [ ] Test at mobile, tablet, and desktop widths

## Definition of Done

- Nav sticks correctly, transitions on scroll, hamburger works on mobile
- Hero renders correctly at all breakpoints with image placeholder visible
- Footer contains all required channels (phone, email, WhatsApp, Instagram)
- CTA scrolls smoothly to contact section
- No console errors

## Open Questions

- Final logo asset: use a text lockup (`Alumbra Comares` in Cormorant Garamond script) until a real logo SVG is provided
