# M17.6 — `/eventos` Standalone Page

## Goal

Split the events surface to its own page (`/eventos.html`). Homepage replaces the eventos carousel section with a CTA card in the secondary page-links row (alongside FAQ and Recursos).

## Dependencies

- M17.5 complete

## PR Scope

- New `eventos.html` — top-level page scaffold following the same pattern as `faq.html`, `como-trabajamos.html`, etc.
- `js/content-loader.js` — add `isEventosPage()` helper and `populateEventosPage()` function rendering all events; remove eventos section from homepage DOMContentLoaded flow
- `<title>`, meta description, og tags for `eventos.html`
- Remove `<section id="eventos">` from `index.html`
- Add CTA card for Eventos into `populatePageLinksSecondary()` — icon 📅, title "Eventos y talleres", desc brief, cta "Ver eventos →"
- `isEventosPage()` branch in DOMContentLoaded

## Tasks

- [ ] Create `eventos.html` page scaffold
- [ ] Add `isEventosPage()` and `populateEventosPage()` to `content-loader.js`
- [ ] Add per-page `<title>` and meta tags
- [ ] Remove eventos section from homepage (loadSection + populateEvents + initCarousel + revealSection)
- [ ] Remove `<section id="eventos">` from `index.html`
- [ ] Add Eventos CTA card to `populatePageLinksSecondary()`
- [ ] Test: `/eventos.html` renders all event cards; homepage shows CTA card in secondary row

## Definition of Done

- `/eventos.html` renders all event items; placeholder dates shown as "Fecha por confirmar"
- Homepage eventos section replaced by CTA card in secondary page-links row
- Nav unchanged (events not in main nav)

## Notes

- If events list is empty, `/eventos.html` shows a friendly "Próximamente nuevos eventos y talleres" message — same as current `populateEvents()` empty-state behaviour
- Carousel on `/eventos.html` is optional — with a small number of events a simple grid may be more appropriate; agent can judge based on item count
