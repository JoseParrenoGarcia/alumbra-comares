# M2 — JSON Content Model

## Goal

Create the full content structure as JSON files so copy can be edited without touching HTML. All sections are populated with real content from the reference material, or explicit placeholders where content is missing.

## PR Scope

- `content/site.json` — top-level content manifest
- `content/hero.json`
- `content/philosophy.json`
- `content/why-alumbra.json`
- `content/team.json`
- `content/services.json`
- `content/events.json`
- `content/testimonials.json`
- `content/contact.json`
- `content/footer.json`

## Content Source

[`assets/reference-docs/reference-content-in-text.md`](../assets/reference-docs/reference-content-in-text.md)

## Tasks

- [x] Define JSON schema for each section
- [x] Populate hero content (Spanish)
- [x] Populate philosophy and pillars content
- [x] Populate why-alumbra section (name meaning + brand rationale)
- [x] Populate three midwife profiles (name, role, bio, quote)
- [x] Populate services list with placeholders for pricing
- [x] Create 2–3 placeholder event entries
- [x] Add testimonials placeholder structure
- [x] Populate contact channels
- [x] Add language key to all string fields for future i18n readiness

## i18n Convention

Each string field that will need translation should use the following structure:

```json
{
  "es": "Texto en español",
  "en": "Text in English",
  "ca": null
}
```

Fields that are not translated yet should use `null` for missing languages.

## Definition of Done

- All JSON files exist and are valid
- Real content from reference material is used where available
- Placeholders are clearly marked with a `"__PLACEHOLDER__"` prefix or a `placeholder: true` flag
- No HTML depends on these files yet — they are purely a content artifact

## Open Questions

- Should pricing be a string placeholder or a structured object with amount + currency fields?
- Should events be a single file with an array, or one file per event?
